# 班級書箱管理系統交接操作手冊

本手冊提供接手的圖書館管理者、行政人員與教師使用。請依自己的身分閱讀對應章節；不需要先懂程式、GitHub 或 Firebase。

---

## 壹、先知道這三件事

1. **一般教師選書**：請開啟公開網站 `https://az159753ymes-cell.github.io/Library-2027/`。教師不需要安裝任何程式，也不需要開啟本機預覽。
2. **管理者修改書箱、輪換或選書時段**：可在公開網站操作。修改完成後，桌機請點左上角、手機請點頂端的 LIBRARY 圖樣，再按「確認儲存」；完成這一步，正式資料與教師端才會更新。
3. **修改按鈕、畫面、程式或圖片**：這是系統維護工作，才需要使用本機預覽。請不要直接雙擊 `index.html`，否則 Google 登入無法正常使用。

> **重要提醒**：公開網站與本機預覽都使用同一份正式 Firestore 資料。本機預覽不是測試資料庫，請勿輸入虛構班級、書箱或選書資料。

---

## 貳、開啟系統

### 一、教師與日常管理者：開啟公開網站

1. 開啟 Chrome、Edge 或 Safari。
2. 在網址列輸入 `https://az159753ymes-cell.github.io/Library-2027/`。
3. 點「使用 Google 帳號登入」，選擇正確的 Google 帳號。
4. 請勿從 LINE 內建瀏覽器直接登入。手機會先顯示「請使用瀏覽器開啟連結，切勿LINE直接點選！」提示；若登入視窗沒有出現，請改用 Chrome、Edge 或 Safari 開啟上述網址。

### 二、系統維護者：開啟本機預覽

本機預覽只用於檢查尚未公開的程式、版面與圖片修改。啟動前，請確認已收到完整的 `Library 2027` 專案資料夾。

1. 開啟 `Library 2027` 專案資料夾。
2. 依序開啟 `tools` 資料夾。
3. 雙擊 `start-local-library.ps1`。
4. 系統會啟動本機服務，並以 Chrome 開啟 `http://localhost:4173/`。
5. 看到登入畫面後，點「使用 Google 帳號登入」。

**請勿使用以下方式開啟系統：**

- 不要直接雙擊 `index.html`。
- 不要在本機預覽中輸入虛構資料，因為它仍會讀寫正式資料。

### 三、本機預覽無法開啟時

1. 確認你開啟的是 `Library 2027\tools\start-local-library.ps1`，不是其他資料夾內同名或舊名稱的檔案。
2. 確認專案資料夾沒有被重新命名、拆散或只複製部分檔案。
3. 若瀏覽器沒有自動開啟，請手動在瀏覽器輸入 `http://localhost:4173/`。
4. 若仍無法開啟，請停止操作正式資料，保留錯誤畫面，交由系統維護者檢查本機預覽環境。

---

## 參、一般教師選書

### 一、登入與確認班級

1. 依「貳、一」開啟公開網站，按「使用 Google 帳號登入」。
2. 選書時段尚未開始時，畫面會顯示「班級書箱」選書時段尚未開放；截止後會顯示已截止。這兩種情況都不能選書，請等待管理者設定或聯絡管理者。
3. 時段開放後，輸入本次選書的 3 位數班級代碼，例如 `701` 或 `801`。
4. 按「進入書箱選擇」。每次重新登入都要再次輸入班級。

### 二、選書、改選與取消

1. 選書表列出書箱編號、書箱名稱、書箱總數與 3 個閱讀時段。每學年第一學期的七年級第一次為不可選的留白時段，不能選書、改選或取消；活動名稱會顯示在書籍摘要。
2. 在要選的閱讀時段，點「可選」即可送出選擇。成功後系統會自動儲存，不需要再按儲存鍵。
3. 同一班級每個時段只能選 1 個書箱；同一學期也不能重複選同一個書箱。若同一時段已被其他班選走，該格不能再選。
4. 要改選時，點同一時段另一個「可選」書箱，確認提示後完成改選。
5. 要取消時，點自己班級號碼右側的磚紅色「×」。取消後該格恢復「可選」，日後由班際輪換安排。
6. 任何已登入教師只要輸入相同班級代碼，就能調整該班原有選擇。因此輸入班級前，務必再次核對班號。

### 三、登出

選書完成後，點右上角「Google 顯示名稱 登出」。本系統不保留登入狀態，下次開啟或重新整理時需要重新登入。

---

## 肆、管理者日常資料維護

### 一、管理者登入與畫面

只有 Google 帳號 `az159753ymes@gmail.com` 能看到管理工作臺。登入後可使用「書箱清冊」、「班際輪換」與「教師選書」。管理者若要登出或切換帳號，請點左下角的小女孩插圖。

### 二、登入後的資料確認頁

管理者登入後，系統會先顯示資料確認頁；這不是寫入資料庫的操作，而是確認目前畫面沒有使用過期或衝突資料。四項內容全部確認後，才可進入「書箱清冊」：

1. 已從 Firestore 伺服器讀取正式清冊。
2. 顯示正式清冊本數、最後編號與系統資料更新時間。
3. 目前學期設定已確認，包括班級數與輪換安排。
4. 教師選書開放時段與已選書內容已確認。

若清冊或學期設定與本機資料不同，頁面會顯示中文差異內容，請逐項選擇保留本機或正式資料後，再按對應的合併按鈕。若仍有管理員尚未儲存的修改，也會在這裡提示；確認頁右下角只會顯示當下需要的操作按鈕。

### 三、最重要的正式儲存步驟

管理者在畫面中按「儲存資料」、「儲存設定」、星星開關、手動指定、隨機或清除，只是先完成目前畫面的修改。**要讓正式資料與教師端更新，最後都必須完成下列步驟：**

書箱清冊上方會顯示資料來源。若顯示「尚未取得正式清冊」、「正式清冊不存在」或「讀取失敗」，請先下載本機清冊備份，並用「重新讀取正式清冊」檢查；此時不可把目前畫面當成正式清冊。若出現本機與正式清冊差異，請逐本選擇保留哪一份，再按「確認合併至目前畫面」。這一步仍未寫入正式資料，還須完成下述 LIBRARY 儲存流程。

1. 點可點擊的 LIBRARY 圖樣：桌機在左上角，手機在頂端導覽列。
2. 確認右側狀態卡顯示「尚未儲存；教師端尚未更新」。
3. 按「確認儲存」。
4. 看到「資料已儲存，教師端已更新」後，才可關閉、重新整理或離開頁面。

沒有修改時，點 LIBRARY 圖樣只會顯示目前資料已儲存，不會重複寫入資料庫。

### 四、書箱清冊

1. 點「書箱清冊」。
2. **新增書箱**：按「新增書箱」，填入編號、書名、尾號與缺號。編號、書名、尾號為必填；缺號與備註可選填。按「儲存資料」後，再依「肆、三」完成正式儲存。
3. **修改書箱**：桌機點選要修改的書箱，右側表單會帶入原資料；手機請點該書箱「備註」右側的鉛筆。再次點同一支鉛筆可收起表單。修改後按「儲存資料」，再依「肆、三」完成正式儲存。
4. **刪除書箱**：點選書箱後按「刪除書箱」。刪除前請先確認該書箱是否仍在使用或已安排輪換，並在刪除後依「肆、三」完成正式儲存。
5. **星星開關**：星星亮起表示可加入輪換，灰色表示暫停輪換。若關閉的書箱已在班際輪換中，系統會警告並在確認後清除既有輪換排程；請先下載備份並確認需要重新安排，再繼續操作。
6. **列印**：手機點右上角列印圖示可快速查看完整清冊，不會開啟手機的系統列印視窗；桌機仍可使用列印功能輸出或另存 PDF。

### 五、班際輪換

1. 點「班際輪換」。右上角下拉選單可切換學期；齒輪按鈕可開啟「輪換系統設定」。
2. **設定學期資料**：在「輪換系統設定」中設定 7 年級與 8 年級班級數，以及發書、第一次交換、第二次交換與回收日期。按「儲存設定」後，再依「肆、三」完成正式儲存。
3. **新增或刪除學期**：點學期下拉選單左側的日曆按鈕，開啟「學期管理」。新增學期前先確認名稱與學期別；刪除學期前，先下載備份並確認該學期不再需要，因為刪除會移除該學期資料。
   學期只分開保存輪換、日期、班級數與選書紀錄；書香清冊為所有學期共用，切換學期不會讓已新增的書香消失。
   過往學期的班際輪換為唯讀，書籍呈淡橘色底；只可切換學期、查閱年級與列印，不能手動輸入、隨機、清除或修改設定。學期管理符號仍保留，但新增或刪除學期須先切回最新學期。離開歷史學期的班際輪換頁面時會自動切回最新學期。
4. **手動指定書箱**：點班級與閱讀時段的儲存格，輸入存在的書箱編號。只輸入有效編號才會指定成功；輸入不存在的編號會保留原排程。第一學期七年級第一次預設為「參觀圖書館＆新生齊步走」，必要時仍可手動覆寫安排書箱；教師端 P1 維持不可選的留白時段。八年級原則上不可指定七年級已讀書箱，唯一例外為 `115-1` 的812班第1本可指定68號書箱。清空既有編號後按 Enter 或移開游標，會移除該格指定。
5. **隨機**：按「隨機」後，系統保留手動指定與教師指定的書箱，並安排其餘空白格。同一本書若在緊接下一時段由任何班級接手，系統會顯示實際接收班級並直接移交，不受班序或年級限制；若指定書籍無法交給班序上的下一班，系統會優先重排原本由圖書館發書的自動格位，補成第1本→第2本→第3本接力。七年級第1本仍為固定活動，第2本→第3本後回圖書館；八年級則於第3本後回圖書館。執行前請先檢查星星關閉的書箱與手動指定是否正確。
6. **清除**：按「清除」只會移除自動安排的排程，會保留管理者手動輸入以及教師來源的指定書箱。清除前請先下載備份並確認範圍。
7. **列印**：手機按「列印」可捲動查看七、八年級總覽與所有班級任務卡，不會開啟手機的系統列印視窗；桌機按「列印」後，仍可在瀏覽器列印視窗選擇印表機或「另存為 PDF」。

### 六、教師選書時段與紀錄清理

1. 點「教師選書」。
2. **開放時段**：按右上角「開放時段」，設定開始與截止日期、時間。按「儲存時段」後，系統會直接寫入教師選書入口資料，並顯示成功或失敗訊息；若畫面仍在讀取，請等待，不要重複操作。
3. **紀錄清理**：這是不可由網站復原的高風險操作。請依序完成：
   1. 點左下角資料夾，下載完整正式 JSON 備份。
   2. 按「紀錄清理」，選擇要清理的學期。
   3. 輸入畫面要求的完整學期名稱。
   4. 再次確認清理範圍，最後按「確認清除」。
4. 清理當學期時，只清除教師選書資料，班際輪換保留。清理已結束學期時，會清除該學期完整教師選書來源資料。任何情況下，都要先下載備份。

### 七、下載備份

管理者可點左下角資料夾，下載完整 Firestore JSON 備份。進行刪除學期、清理紀錄、關閉已排程書箱星星，或大型功能、登入、同步與權限變更前，都應先下載備份。

---

## 伍、系統維護與公開發布

本章只給負責修改網站程式、版面或圖片的人使用。日常書箱、輪換、學期與選書時段資料修改，不需要 GitHub。

### 一、先判斷修改類型

| 修改內容 | 完成方式 |
| --- | --- |
| 書箱、輪換、學期、教師選書時段 | 在管理畫面修改，接著點 LIBRARY 圖樣，再按「確認儲存」。不需要上傳 GitHub。 |
| 按鈕、畫面、文字、程式邏輯、`index.html`、`assets` 圖片或素材 | 用本機預覽驗證後，才可上傳 GitHub，等待 GitHub Pages 發布。 |
| `firestore.rules` | 除了上傳程式，還必須另外發布 Firestore 規則。 |

### 二、功能或版面修改流程

1. 先確認目前有哪些未處理的修改，記錄本次要變更的功能與檔案。
2. 若涉及資料同步、登入、權限或大型功能，先下載正式 Firestore JSON 備份，並建立可還原的專案壓縮備份。
3. 依「貳、二」開啟本機預覽，檢查畫面與功能。請勿以虛構資料測試。
4. 若影響教師選書、登入或資料寫入，除管理者帳號外，也要以非管理員教師帳號實際驗證。
5. 本機驗證完成後，才向系統管理者提出「上傳到 GitHub」的請求。
6. 等待 GitHub Actions 的 GitHub Pages 發布成功後，再開啟公開網站，以非管理員教師帳號驗證登入、資料讀取與受影響的操作。

### 三、版本管理與重大改版備份

1. 每次公開發布後，維護者須在 `CHANGELOG.md` 記錄功能與檔案異動。
2. 若改動涉及資料結構、資料同步、登入、權限、Firestore 規則、整個模組重整或大量刪除，屬於重大改版。開始前必須先由管理者下載完整 Firestore JSON 備份。
3. 接著由系統維護者執行 `tools/create-major-backup.ps1`，指定即將改版的版本號與說明。系統會在專案上層的 `C:\Users\wzm\Desktop\圖書館系統版本控制` 建立可還原的專案 ZIP 備份，不會覆寫既有備份。
4. 小型文字、色彩、間距或單一按鈕版面調整，不必建立壓縮備份；但仍要完成本機預覽與 Git 版本紀錄。
5. 若需還原，先停止修改，解壓對應版本備份到新的資料夾核對內容，再由系統維護者評估是否回復 Git 提交與 Firestore JSON 資料；不要直接覆蓋正在使用的專案或正式資料。

### 四、交接時必須交付的項目

- 完整的 `Library 2027` 專案資料夾，不可只交付單一 `index.html`。
- 本操作手冊與系統架構書。
- `CHANGELOG.md` 與最近一次重大改版備份。
- 公開網站網址：`https://az159753ymes-cell.github.io/Library-2027/`。
- 管理者帳號的交接方式，應依學校資訊安全規範辦理，不要將密碼寫入本手冊。
- 最近一次正式 JSON 備份與重大變更時建立的可還原專案壓縮備份。

---

## 陸、常見問題

### 問：換電腦後資料會不見嗎？

答：不會。正式書箱、輪換、學期與教師選書資料存於 Firestore。使用另一台電腦登入後會讀到同一份正式資料；瀏覽器暫存不能當作備份。

### 問：我按了「儲存資料」，教師端為什麼沒有更新？

答：請再點 LIBRARY 圖樣，並按「確認儲存」。表單的「儲存資料」不等於正式發布到教師端。

### 問：公開網站的按鈕或畫面沒有更新，該怎麼辦？

答：這是程式或版面修改，必須完成本機驗證、上傳 GitHub，並等 GitHub Pages 發布成功。只改本機檔案不會更新公開網站。

### 問：Google 登入失敗怎麼辦？

答：確認網路正常後，用 Chrome、Edge 或 Safari 開啟公開網站；不要從 LINE 內建瀏覽器登入。若瀏覽器阻擋登入視窗，請允許彈出式視窗後再試。

### 問：登入後出現英文錯誤怎麼辦？

答：先關閉此系統的全部分頁，再用 Chrome、Edge 或 Safari 重新開啟公開網址並登入。登入頁下方也會顯示相同提醒。

### 問：輸入數字時切到全形輸入法怎麼辦？

答：書箱編號與班級代碼欄位會自動將全形數字轉為半形數字。
