# 高雄市立五甲國中「班級書箱管理系統」系統架構與技術設計書

* **版本號**：v4.1.63
* **更新日期**：2026-09-14
* **設計目標**：單頁網頁架構，結合 Firebase Authentication 與 Firestore，提供管理者工作臺及受限的教師選書入口。

---

## 一、 設計理念與目標
本系統專為國中圖書館之「班級書箱管理」與「班際閱讀輪換」需求設計。系統採用 Firebase Google 登入、Firestore 權限規則與瀏覽器 LocalStorage 快取。管理者可維護完整清冊與輪換資料；一般教師僅能讀取選書所需資料並選擇自己輸入班級的書箱時段。
介面採用**溫馨繪本與典雅閱讀風**配色（典雅森林綠、大自然木質、溫潤羊皮紙白），並針對大螢幕顯示器與紙張列印進行雙軌優化。

---

## 二、 技術架構與開發規範
| 項目 | 使用技術 / 工具 | 說明與用途 |
| :--- | :--- | :--- |
| **網頁結構** | HTML5 | 語意化標籤，建立清晰好讀的骨架。 |
| **視覺樣式** | Tailwind CSS (CDN v3) + 原生 CSS | 響應式排版佈局與主題配色，微互動設計（保證極致簡約與穩定）。 |
| **系統字型** | Google Fonts (Noto Sans TC, Zen Maru Gothic) | 提供溫潤舒適的字體體驗。 |
| **業務邏輯** | Vanilla JavaScript (ES6+) | 純前端表格渲染、全半形轉換、學期切換與管理、輪換演算法與報表列印。 |
| **登入與權限** | Firebase Authentication＋Firestore Rules | Google 帳號登入；僅已驗證的 `az159753ymes@gmail.com` 可使用管理功能。 |
| **資料儲存** | Firestore＋HTML5 LocalStorage | Firestore 是本機預覽與公開網站共用的唯一正式資料來源；LocalStorage 保留本機清冊供失敗時備份及與正式清冊比對，不代表已同步。 |
| **公開網站** | GitHub Pages | `main` 分支上傳後由 GitHub Actions 部署公開網站；Firebase 保留 Google 登入與即時 Firestore 資料。 |
| **報表輸出** | CSS @media print＋手機完整內容檢視 | 桌機支援 A4 直向（輪換排程總覽表）精準滿版列印輸出；手機的列印入口改為即時、可捲動的完整內容檢視。 |

---

## 三、 系統檔案結構說明

```
圖書館系統/
├── index.html            # 主程式檔案（包含介面 HTML、CSS 樣式與完整 JS 業務邏輯）
├── assets/                # 公開網站使用的圖片與其他靜態素材
├── 114書箱.xlsx           # 目前內建書箱清冊的原始匯入資料，供日後核對使用
├── 系統架構書.md          # 系統架構與技術設計說明文件（本檔案）
├── 操作手冊.md            # 提供給管理員與教師的操作說明文件
├── firebase.json          # Firebase CLI 的 Firestore 規則設定
├── firestore.rules        # Firestore 角色與資料存取規則
├── CHANGELOG.md           # 版本變更紀錄檔
├── .github/workflows/
│   └── deploy-pages.yml   # main 推送後發布 GitHub Pages 的工作流程
└── tools/
    ├── local-preview-server.js  # localhost:4173 本機預覽服務
    └── start-local-library.ps1  # 啟動本機服務並開啟 Chrome 的啟動腳本
    └── create-major-backup.ps1  # 重大改版前建立可還原壓縮備份
```

---

## 四、 維護與發布規範

* **正式儲存與快取**：Firestore 是唯一正式資料來源；本機預覽與公開網站共用同一份正式資料。LocalStorage 僅作短暫快取，不能作為獨立草稿或備份。
* **本機預覽**：`tools/start-local-library.ps1` 會啟動 `tools/local-preview-server.js`，並開啟 `http://localhost:4173/`。本機可驗證尚未公開的程式畫面與功能，但不可輸入虛構資料，因為會讀寫正式 Firestore。
* **資料修改與程式修改**：管理員調整書箱、輪換、學期或教師選書時段後，按左上角 LIBRARY 圖樣及「確認儲存」，即可更新正式資料與教師端。修改 `index.html` 或 `assets/` 的功能、樣式、圖片，則必須推送 `main`，由 GitHub Actions 發布新版網站。
* **正式公開設定**：GitHub 儲存庫為公開的 `az159753ymes-cell/Library-2027`；GitHub Pages 使用 GitHub Actions 工作流程，正式網址為 `https://az159753ymes-cell.github.io/Library-2027/`。Firebase Authentication 必須保留 `az159753ymes-cell.github.io` 為授權網域。
* **規則發布例外**：只有 `firestore.rules` 變更時，才需以 Firebase CLI 發布 Firestore 規則；一般畫面、功能與素材改動不需要發布規則。
* **備份與驗收**：功能架構、資料同步、登入或權限的大型改動前，先下載管理員的正式 Firestore JSON 備份，再用 `tools/create-major-backup.ps1` 建立可還原專案壓縮備份；目前備份存放於專案上層的 `C:\Users\wzm\Desktop\圖書館系統版本控制`，不納入 Git。公開後除管理員外，必須以非管理員教師帳號驗收登入、資料讀取與受影響的操作。
* **本機 Git 設定**：本機 `origin` 若仍保留儲存庫改名前的網址，應在維護作業中更新；公開網址與發布狀態仍以 GitHub Pages 設定為準。
* **素材維護**：`assets/` 僅保留目前 `index.html` 直接使用的公開素材；更換圖片後，先確認新檔已被引用並完成本機預覽，再刪除舊版。原始書箱資料 `114書箱.xlsx` 雖不會在瀏覽器執行時載入，仍保留作為 `INITIAL_BOOKS` 的核對依據。
* **版本紀錄**：`CHANGELOG.md` 僅保存目前公開版本的異動明細。每次重大改版必須先建立備份、完成驗收後再補齊此紀錄。

---

## 五、 核心模組功能與演算法

### 1. 學期切換與管理模組 (Semester Manager & Isolation Module)
* **機制**：系統支援自訂新增與刪除學期（例如：114-1、114-2、115-1）。點擊學期選單左側日曆圖示即可進入「學期管理」視窗，維護學期清單。書香清冊跨所有學期共用；輪換、日期、班級數與選書紀錄則維持學期獨立。
* **暫存金鑰（LocalStorage Keys）**：
  * 學期清單：`wujia_semesters_list_v32`
  * 共用書箱清冊：`wujia_book_trunks_shared_v33`
  * 上次伺服器確認清冊版本：`wujia_book_trunks_server_signature_v34`
  * 輪換排程：`wujia_rotation_schedule_{currentSemester}_v32`
  * 日期設定：`wujia_rotation_dates_{currentSemester}_v32`
  * 班級數設定：`wujia_grade_class_counts_{currentSemester}_v32`
  * 八年級歷史閱讀紀錄：`wujia_grade8_history_books_v32`
* **同步原則**：上述金鑰用於瀏覽器本機保留；管理員明確確認儲存且伺服器回讀通過後，Firestore 的 `system/catalog` 才是跨裝置共用清冊的正式資料來源，學期文件只保存該學期設定與排程。
* **儲存防護**：管理頁不會因載入、重新整理或切換學期而自動寫入。伺服器正式清冊或學期設定未取得時，頁面明示來源並禁止正式儲存；本機與正式清冊不同時須逐本確認合併，避免快取覆寫其他裝置資料。
* **歷史輪換唯讀**：以學期清單建立順序判定，最新學期以外的輪換卡片皆為淡橘色；隱藏隨機、清除及輪換設定入口，移除卡片編輯事件，並在修改函式再次阻擋。保留學期管理符號、學期選擇、年級查閱與列印；離開歷史輪換頁面自動回到最新學期。

### 2. 書箱清冊管理模組 (Book Catalog Module)
* **跨學期共用**：新增、修改、刪除書箱及星星輪換狀態後，所有學期都會使用同一份清冊；切換或新增學期不會建立另一份書香清冊。
* **資料欄位**：
  * `id`：唯一識別碼 (Number / Timestamp)
  * `code`：書箱編號 (String，例如：`1`)
  * `title`：書名 (String，系統自動去除《》符號)
  * `maxNum`：尾號 / 書本到幾號 (String，例如：`31`)
  * `missing`：缺號清單 (String，例如：`23 21`)
  * `total`：實際數量 / 共幾本 (String，自動計算：尾號 - 缺號本數)
  * `note`：備註說明 (String)
  * `inRotation`：是否加入班際輪換 (Boolean，預設 `true`；若為 `false` 則星星熄滅，不參與輪換)
* **特色功能**：
  * **星星即時切換**：表格備註欄位內建 SVG 五角星星圖示（金色代表開啟加入輪換、灰色代表關閉暫停輪換），點擊即可即時開關。
  * **已排程防護通知**：當關閉一顆已被安排在班際輪換中的書箱星星時，系統會主動彈窗提示，並在使用者確認後自動清除輪換排程以便重新分配。
  * **智慧全形轉換**：全形數字與符號自動轉為半形數字、自動扣減缺號計算實際本數。

### 3. 教師選書與資料隔離模組
* **角色導向**：已驗證的 `az159753ymes@gmail.com` 登入後固定進入書箱清冊，保有清冊、輪換與教師選擇頁面；其他 Google 帳號固定進入教師選書頁，需先輸入3位數班級。
* **登出入口**：管理員不顯示管理身分與頁首登出控制，改由左下角小女孩插圖登出並切換帳號；非管理員在右上角點選「Google 顯示名稱 登出」即可登出。
* **手機版排版**：螢幕寬度 `760px` 以下以頂部導覽取代固定側欄；教師選書改用書箱卡片逐列呈現 3 個時段，管理員書箱清冊採固定左右雙欄，左側書籍卡片與右側新增／修改表單的欄寬為 `6:4`，不使用橫向滑動。手機書箱卡片顯示缺號、尾號、數量與備註，不顯示「輪換狀態」文字，但保留星號切換班際輪換。管理員可點擊手機頂端的 LIBRARY 圖樣開啟與桌機共用的儲存狀態視窗；視窗依螢幕寬度附掛於手機頂端列或桌機側欄，教師端 LIBRARY 則維持純識別圖樣。班際輪換改為一班一張卡片，內含三個閱讀時段並重用桌機的指定與清空操作。手機「列印」入口會立即開啟可捲動的完整內容，不啟動系統列印服務；齒輪設定視窗的交換日期欄位在手機改為單欄並可在視窗內捲動。教師端在手機只保留頂部「教師選書／登出」，完成班級確認後顯示「教師選書（班級書箱）」且括號內文字採書箱資訊色；桌機頁首與姓名登出控制維持原樣。手機書箱卡片的書名與「書箱X號（Y本）」同列左右呈現，所有卡片後以3欄×2列摘要表格呈現3個時段日期與對應選書結果；摘要的日期列與結果列統一為薄荷淺綠底，日期與未選結果採深綠字，已選書箱採磚紅字；僅手機登入前會先顯示「請使用瀏覽器開啟連結，切勿LINE直接點選！」的花草框提示；英文錯誤提醒圖片僅顯示於手機登入卡片下方，桌機登入頁不顯示。首次確認班級卡片在手機可視畫面垂直置中。
* **三時段選書與固定活動**：選書表將每個書箱拆分為書箱編號、書箱名稱、書箱總數三欄；每學期每本書箱分為 `p1`、`p2`、`p3` 3個獨立名額。唯第一學期七年級 `p1` 為不可選的留白時段，教師端摘要顯示「參觀圖書館＆新生齊步走」；管理員輪換表預設顯示活動，但仍可因特殊需要手動指定書箱，該指定不會揭露至教師端。
* **本班選書快速顯示**：教師確認班級後，系統會平行讀取該班3個時段的固定選書索引，先呈現本班已選書箱與班級號碼；全學期即時監聽隨後補齊其他班級狀態，不會寫入或更動既有選書。
* **最新學期與開放時段**：所有使用者先以 Google 帳號登入，再依身分顯示管理或教師畫面。教師選書固定使用學期管理中最後新增並發布的學期，不顯示學期選擇器；時段尚未設定或尚未開始時，非管理員登入後中央顯示「班級書箱」選書時段尚未開放，並隱藏班級輸入與選書表；截止後中央框框顯示該學期「班級書箱」選書開放時段已截止。
* **學期資料整理**：管理員先下載完整 Firestore JSON 備份，再輸入學期名稱確認清理。已結束學期會清除 `teacherPortal`、`teacherBooks`、`selections`、`classPeriodSelections` 與 `classBookSelections`；當學期會清除全部 `selections`、`classPeriodSelections` 與 `classBookSelections`，保留教師入口、書箱、開放時段與班際輪換。系統沒有「測試」資料標記，不會區分測試與正式選書。每批最多刪除 `400` 筆；失敗時停止，不自動重試。
* **時段儲存回饋**：儲存按鈕在送出期間會鎖定；成功後關閉視窗並提示完成；若 Firestore 寫入失敗，視窗保留原本日期與時間，並顯示可理解的錯誤原因。
* **教師來源匯入**：最新學期時段截止後，系統自動將合格教師選擇匯入班際輪換；選書開放期間不提供手動匯入。匯入資料標記為 `selectionSource: 'teacher'`，不等同管理員手動指定，且不會覆寫 `manualSpecified: true` 的格位。
* **教師改選與取消指定**：教師在開放時段內可點選另一個可選書箱，改選同一時段原書箱；系統先顯示前後書箱確認。任何已登入教師輸入相同班級後，均可接手改選或點選該班號右側的「×」取消指定，不受最初選書 Google 帳號限制。第一學期七年級 `p1` 為固定活動，不適用選書、改選或取消。其餘時段的改選與取消均以原子交易同步資料；取消會刪除選書、班級時段與班級書箱對應，讓該格回到未指定狀態，維持每班每時段1本及全學期不重複書箱。
* **教師選書視覺一致性**：選書表格的資料列統一採純白底色，僅以綠色「可選」按鈕、深綠班級標籤與獨立的磚紅色「×」圓角取消標籤表達可操作狀態。
* **指定書籍防誤觸**：教師來源書籍與管理員手動指定分開標記。教師來源可隨匯入同步新增、改選與取消；管理員實際輸入有效書號後才轉為手動指定。按「清除」時，兩種指定都會保留，只移除系統自動安排的排程。
* **清除後路徑重建**：保留管理員手動指定與教師來源指定，並清除其餘自動安排後，系統會重新執行七、八年級的移交日期與起訖地點計算，避免保留書籍遺失 `dueDate` 而顯示「未定」。
* **閱讀時段計算**：教師選書摘要的 `p1`、`p2`、`p3` 分別從發書日、第一次交換日、第二次交換日起算，到下一批圖資股長收書日當日為止。前兩批依教師檢查日所在週的週一計算；期末依期末收書工作日計算。
* **借閱與輪換資格**：書箱有缺號或 `inRotation` 為 `false` 時，仍顯示於教師選書總覽，但三個時段均留白，且不會匯入或分派至班際輪換；資料庫交易同時檢查 `teacherBooks` 的 `available` 狀態。
* **資料分離**：完整資料存於 `semesters` 與 `system`，僅管理者可讀寫；教師讀取 `teacherPortal`、`teacherBooks` 與 `selections` 所需的最小資料集，其中 `teacherBooks` 同步書箱編號、名稱、總數與可選狀態。

### 4. 跨年級聯合閉環演算法與全域防撞書驗證模組 (Joint Rotation & Collision-Free Module)
* **星星關閉排除規則（嚴格排除暫停輪換之書箱）**：
  * 電腦隨機分配演算法會自動過濾 `inRotation !== false` 之合格書箱，凡星星熄滅（暫停輪換）之書箱，絕對不會被隨機分派至任何班級。
* **跨班級／跨年級實體書箱接力機制（最大化減少回流圖書館）**：
  * 移交以「同一本書在緊接下一時段的實際接收班級」為準，不受班序或年級限制。例如某班第2本與另一班第3本為同一書箱時，前者直接移交給後者，不回圖書館。
  * 教師選書與管理員手動指定優先保留；若同一本指定書已鎖定第1本與第3本而缺少第2本，系統會在合格空白班級補入第2本，完成連續接力。無指定限制的書箱仍優先依原本班序安排。
  * 當指定的第1本或第2本無法依班序交給下一班時，系統會優先重排原本「由圖書館發書」的自動格位，補齊第1本→第2本→第3本的完整接力；不會覆寫教師或管理員指定。
  * 七年級第1本維持「參觀圖書館＆新生齊步走」；七年級書箱於第2本→第3本後回館，八年級書箱於第1本→第2本→第3本後回館。
  * **八年級七年級歷史閱讀防重**：電腦隨機分派 100% 嚴格避開過去七年級已讀過之書籍，確保國中階段不重複閱讀同一本書。唯一例外為 `115-1` 的812班第1本可重讀68號書箱；此例外不適用其他班級、時段、書箱或學期。
* **終極全域防護驗證機制 (validateGlobalScheduleNoDuplicates)**：
  * 分配完成後自動全校全班時段進行雙重檢查，確保跨年級與跨時段 100% 零衝突。

---

## 六、 未來系統擴充規劃建議

1. **自動化異地備份**：在既有 Firestore JSON 手動備份外，規劃可驗證、可還原的定期異地備份機制。
2. **多語系支援**：若有雙語教學需求，可擴充英文介面切換，但不得影響既有繁體中文操作流程。
3. **維護紀錄強化**：持續以 `CHANGELOG.md` 記錄已發布功能，並在大型資料、同步或權限變更時同步更新本文件與操作手冊。
